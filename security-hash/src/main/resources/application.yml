spring:
  profiles:
    active: actuator, console, config, ${PROFILE:test}
    group: 
      prod: ${CACHE_MODE:none} # cluster
      # test: 
  application:
      name: security-HASH

server:
  port: ${SECURITY_HASH:8000}
  forward-headers-strategy: framework