spring:
  profiles:
    active: actuator, console, config, ${PROFILE:test}
    #group: 
      #prod: 
      #test: 
  application:
      name: Security-HASH

server:
  port: ${SECURITY_HASH:8000}
  forward-headers-strategy: framework